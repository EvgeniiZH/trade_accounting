# Исправление отображения таблиц - ИТОГОВЫЙ ОТЧЁТ

## 🔍 Найденная проблема

На странице **редактирования расчёта** таблица показывала только 2 колонки вместо 5 из-за конфликта CSS правил.

## ⚠️ Причина

**Конфликт CSS файлов:**

1. `static/css/pages/calculations_list.css` — содержал стили для `#calculation-table` с фиксированными ширинами колонок
2. Этот файл подключался в `base.html` и применялся ко **ВСЕМ** страницам
3. Он перебивал правильные стили из `create_calculation.css`

## ✅ Решение

### 1. Удалил конфликтующие правила

**Файл:** `static/css/components/table.css`

Удалил старые правила для `#calculation-table` (строки 349-385), так как они дублировались и конфликтовали с новыми стилями.

### 2. Убрал calculations_list.css из глобальной загрузки

**Файл:** `trades/templates/base.html`

- Удалил `<link rel="stylesheet" href="{% static 'css/pages/calculations_list.css' %}?v=20251123b">`
- Добавил блок `{% block extra_css %}{% endblock %}` для страничных стилей

### 3. Добавил calculations_list.css только на страницу списка расчётов

**Файл:** `trades/templates/trades/calculations_list.html`

```django
{% block extra_css %}
    <link rel="stylesheet" href="{% static 'css/pages/calculations_list.css' %}?v=20251123c">
{% endblock %}
```

### 4. Обновил версию CSS

Изменил версию table.css с `v=20251123b` на `v=20251123c` для принудительной перезагрузки в браузерах.

## 📦 Задеплоенные изменения

✅ `static/css/components/table.css` — удалены конфликтующие правила
✅ `trades/templates/base.html` — убран calculations_list.css, добавлен блок extra_css
✅ `trades/templates/trades/calculations_list.html` — добавлено подключение calculations_list.css через extra_css

## 🧪 Как проверить

### Локально (http://127.0.0.1:8000)

1. **Откройте** http://127.0.0.1:8000/calculations/101/ (или любой расчёт)
2. **Нажмите** Ctrl+Shift+R для жёсткой перезагрузки
3. **Проверьте:**
   - ✅ Таблица показывает **5 колонок**: №, Товар, Цена, Количество, Выбрать
   - ✅ Все колонки видны (возможна горизонтальная прокрутка)
   - ✅ Поиск работает без потери фокуса
   - ✅ Кнопка "Очистить" очищает поиск

### На production (https://kiteh.ru)

1. **Откройте** https://kiteh.ru/calculations/103/
2. **Нажмите** Ctrl+Shift+R
3. **Проверьте то же самое**

### Дополнительная проверка списка расчётов

1. **Откройте** http://127.0.0.1:8000/calculations/ или https://kiteh.ru/calculations/
2. **Убедитесь**, что таблица списка расчётов отображается правильно (9 колонок)
3. **Проверьте**, что стили не сломались

## 📊 Ожидаемый результат

### Страницы создания/редактирования расчёта

Таблица с **5 колонками**:
- **№** (50px) — порядковый номер
- **Товар** (auto, min 200px) — название товара с переносом
- **Цена** (130px) — цена товара (справа)
- **Количество** (120px) — input для ввода количества
- **Выбрать** (100px) — чекбокс для выбора товара

### Страница списка расчётов

Таблица с **9 колонками** (без изменений):
- №, Чекбокс, Название, Товаров, Стоимость, С наценкой, Дата, Автор, Действия

## 🚨 Если проблема осталась

1. **Очистите кеш браузера:**
   - F12 → Network → галочка "Disable cache"
   - Ctrl+Shift+Delete → Очистить кеш за всё время

2. **Проверьте DevTools:**
   - F12 → Network
   - Перезагрузите страницу
   - Найдите `table.css?v=20251123c`
   - Убедитесь, что Status = 200 (не 304)

3. **Проверьте в режиме инкогнито:**
   - Ctrl+Shift+N
   - Откройте страницу заново

## 📝 Технические детали

### Порядок применения CSS (после исправления):

1. `base.css`
2. `layout.css`
3. `table.css` ← общие стили таблиц
4. `form.css`
5. `alerts.css`
6. `checkbox.css`
7. `animation.css`
8. `login.css`
9. **[страничные стили через extra_css]:**
   - `calculations_list.css` — только на `/calculations/`
   - `create_calculation.css` — только на `/calculations/create/` и `/calculations/<id>/`

### Почему это работает:

- Страничные CSS загружаются **после** базовых
- Более специфичные правила из страничных CSS перебивают общие
- Нет конфликтов между `calculations_list.css` и `create_calculation.css`

