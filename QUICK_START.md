# Trade Accounting - Быстрый старт для разработки 🚀

## Текущее состояние (22.11.2025)

✅ **Окружение полностью настроено!**

- Python: 3.11.8
- Django: 5.2.7  
- База данных: SQLite (local) / PostgreSQL (docker/prod)
- Суперпользователи: 3 (уже есть)
- Dev-сервер: **http://127.0.0.1:8000** (уже запущен в терминале 3)

## Как начать работу

### 1. Простой способ (уже готово!)

Сервер уже запущен! Просто открой браузер:
```
http://127.0.0.1:8000
```

Учётные данные суперпользователей — спроси у владельца проекта или создай нового (см. ниже).

### 2. Работа с Makefile (быстрые команды)

```powershell
# Показать все доступные команды
make help

# Запустить dev-сервер (если нужно перезапустить)
make dev

# Применить миграции
make migrate

# Создать новые миграции
make makemigrations

# Запустить тесты
make test

# Открыть Django shell
make shell

# Создать суперпользователя
make createsuperuser

# Собрать статику
make collectstatic

# Очистить кеш и .pyc файлы
make clean
```

### 3. Docker (альтернатива)

Если хочешь работать через Docker:

```powershell
# Запустить все сервисы (web + postgres + redis)
make docker-up

# Применить миграции в Docker
make docker-migrate

# Посмотреть логи
make docker-logs

# Остановить
make docker-down
```

## Структура файлов окружения

- `.env` — **локальная разработка** (SQLite, DEBUG=1) ✅ УЖЕ СОЗДАН
- `env.example` — шаблон для копирования
- `.env.production` — для продакшн-сервера (создаётся на сервере)
- `.env.staging` — для staging-сервера (создаётся на сервере)

## Доступные адреса (local)

- **Главная:** http://127.0.0.1:8000/
- **Админка:** http://127.0.0.1:8000/admin/
- **Список товаров:** http://127.0.0.1:8000/
- **Расчёты:** http://127.0.0.1:8000/calculations/
- **Создать расчёт:** http://127.0.0.1:8000/create/
- **Управление пользователями:** http://127.0.0.1:8000/users/

## Порядок работы с фичами

1. **Создай ветку для новой фичи:**
   ```powershell
   git checkout -b feature/название-фичи
   ```

2. **Внеси изменения, протестируй:**
   ```powershell
   make test
   python manage.py runserver  # или make dev
   ```

3. **Зафиксируй изменения:**
   ```powershell
   git add .
   git commit -m "Описание изменений"
   ```

4. **Создай PR в develop:**
   ```powershell
   git push origin feature/название-фичи
   # Затем создай Pull Request на GitHub в ветку develop
   ```

## Автодеплой

- **Staging:** автоматически при push в `develop`
- **Production:** вручную workflow "Release" или push в `main`

## Полезные команды Django

```powershell
# Проверить конфигурацию
python manage.py check

# Показать все URL
python manage.py show_urls  # (если установлен django-extensions)

# Просмотр миграций
python manage.py showmigrations

# Создать дамп данных
python manage.py dumpdata > backup.json

# Загрузить дамп
python manage.py loaddata backup.json

# Очистить БД (осторожно!)
python manage.py flush
```

## Если что-то пошло не так

### Сервер не запускается
```powershell
# Проверь, что виртуальное окружение активно
.\venv\Scripts\activate

# Проверь зависимости
pip install -r requirements.txt

# Проверь миграции
python manage.py migrate
```

### Ошибки статики
```powershell
make collectstatic
# или
python manage.py collectstatic --noinput
```

### Нужно пересоздать БД
```powershell
# Удали db.sqlite3
rm db.sqlite3

# Примени миграции заново
python manage.py migrate

# Создай суперпользователя
python manage.py createsuperuser
```

## Контакты и документация

- **Полная документация:** `DEVELOPMENT.md`
- **Deployment:** `README.md` (раздел Production)
- **Улучшения:** `IMPROVEMENTS.md`
- **Security:** `SECURITY_RECOMMENDATIONS.md`

---

**Всё готово для разработки! 🎉**

Сервер запущен, окружение настроено, можно кодить! 💻

