# Чеклист для тестирования страниц создания/редактирования расчёта

## 🚀 Запуск локального сервера

```bash
cd C:\dev\trade_accounting
python manage.py runserver 127.0.0.1:8000
```

## 👤 Вход в систему

- URL: http://127.0.0.1:8000/login/
- Логин: `testuser`
- Пароль: `test123`

(Или используйте своего существующего пользователя)

## ✅ Тестирование страницы создания расчёта

### URL: http://127.0.0.1:8000/calculations/create/

#### 1. Проверка отображения таблицы
- [ ] Таблица показывает **5 колонок**: №, Товар, Цена, Количество, Выбрать
- [ ] Все колонки видны без горизонтальной прокрутки (или с ней, но все колонки присутствуют)
- [ ] Колонка "Товар" не обрезается

#### 2. Проверка поиска
- [ ] Поле поиска находится в header (вне таблицы)
- [ ] При вводе текста в поиск (например, "адгез"):
  - Список товаров обновляется через ~500мс
  - Фокус НЕ слетает с поля поиска
  - Найденные товары подсвечиваются
- [ ] Кнопка "Очистить" работает и очищает поиск
- [ ] При поиске `page` сбрасывается на 1
- [ ] При поиске `page_size` сохраняется (проверьте URL после поиска)

#### 3. Проверка фильтра "Показать только выбранные"
- [ ] Выберите несколько товаров чекбоксами
- [ ] Нажмите "Показать только выбранные"
- [ ] Отображаются только выбранные товары
- [ ] Повторное нажатие показывает все товары

#### 4. Проверка пагинации
- [ ] Пагинация работает
- [ ] При смене количества на странице (10/25/50/100/200):
  - Список обновляется
  - `page_size` сохраняется в URL
- [ ] При поиске с изменённым `page_size` значение сохраняется

#### 5. Проверка консоли браузера (F12 → Console)
- [ ] Нет ошибок JavaScript при загрузке страницы
- [ ] Нет ошибок при вводе текста в поиск
- [ ] Нет ошибок при клике на фильтры

## ✅ Тестирование страницы редактирования расчёта

### URL: http://127.0.0.1:8000/calculations/<ID>/

(Создайте сначала расчёт или откройте существующий)

#### Проверьте те же пункты, что и для создания:
- [ ] 5 колонок видны
- [ ] Поиск работает
- [ ] Фокус не слетает
- [ ] `page_size` сохраняется
- [ ] Нет ошибок в консоли

## 🐛 Если обнаружены проблемы

### Проблема: Таблица показывает только 2 колонки
**Решение:**
1. Откройте DevTools (F12) → Network
2. Очистите кеш (Ctrl+Shift+Delete)
3. Перезагрузите страницу (Ctrl+F5)
4. Проверьте, что загружается `create_calculation.css?v=20251123b`

### Проблема: Фокус слетает при вводе в поиск
**Проверьте:**
1. Откройте DevTools (F12) → Console
2. Введите текст в поиск
3. Посмотрите, есть ли ошибки типа "Element not found"

**Если ошибка есть:**
- Поле поиска должно быть **вне** контейнера `#create-calculation-container`
- Проверьте в Elements (DevTools), что `#search-input` находится в `.page-header`

### Проблема: Поиск не работает вообще
**Проверьте:**
1. В консоли браузера введите: `document.querySelector('#search-input')`
2. Должен вернуться элемент input, а не `null`
3. Проверьте, загружен ли `create_calculation.js?v=20251123b`

## 📊 Ожидаемое поведение

### При вводе "адгез" в поиск:
1. Через 500мс список обновляется
2. Показываются только товары со словом "адгез" в названии
3. Слово "адгез" подсвечивается жёлтым (тег `<mark>`)
4. URL обновляется: `?search=адгез&page=1&page_size=<текущий>`
5. Фокус остаётся в поле поиска

### При изменении page_size на 50:
1. Список обновляется, показывая 50 товаров
2. URL: `?page_size=50`
3. При последующем поиске: `?search=текст&page=1&page_size=50`

## 🔧 Если всё работает локально, но не на production

Значит проблема в кеше браузера на production. Попросите пользователей:
1. Открыть DevTools (F12)
2. Зайти в Network
3. Поставить галочку "Disable cache"
4. Перезагрузить страницу (Ctrl+Shift+R)

