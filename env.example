# Django Settings
# ===============
# Важно: Скопируйте этот файл в .env или .env.production и заполните реальными значениями
# НИКОГДА не коммитьте файлы .env в репозиторий!

# Django
SECRET_KEY=your-secret-key-here-generate-with-django-secret-key-generator
DEBUG=False
ALLOWED_HOSTS=kiteh.ru,www.kiteh.ru,89.111.153.46

# Database (для production)
# Для локальной разработки можно использовать SQLite (закомментируйте эти строки)
DATABASE_ENGINE=django.db.backends.postgresql
POSTGRES_DB=trade_accounting
POSTGRES_USER=django
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_HOST=db
POSTGRES_PORT=5432

# Для локальной разработки с SQLite (раскомментируйте, если используете SQLite)
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

# Sentry (опционально, для мониторинга ошибок)
SENTRY_DSN=

# Redis (для production, используется для кэширования и сессий)
# Redis настраивается автоматически через docker-compose

# Local Development
# =================
# Для локальной разработки можете использовать:
# DEBUG=True
# DATABASE_ENGINE=django.db.backends.sqlite3
# DATABASE_NAME=db.sqlite3

